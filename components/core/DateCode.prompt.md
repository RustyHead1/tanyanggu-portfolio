The brand's YYYY.MM date stamp — quiet mono text that anchors a work's metadata line.

```jsx
<DateCode>2025.12</DateCode>
<DateCode tone="fg" size="20px">2024.09</DateCode>
```

Tones: `dim` (default) / `muted` / `fg`. Always dot-separated `YYYY.MM`. Pairs with `<TagRow>` in a `<MetaRow>`.
