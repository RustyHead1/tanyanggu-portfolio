Small uppercase, wide-tracked label that sits above section titles — one of the few uppercase moments in the system.

```jsx
<Eyebrow>Selected Works</Eyebrow>
<Eyebrow tone="accent">Now Showing</Eyebrow>
```

Tones: `dim` (default) / `faint` / `accent` / `fg`. Keep the text short — two or three words.
