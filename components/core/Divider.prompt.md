A quiet hairline rule using the soft 14%-opacity line, optionally with an uppercase label set into it.

```jsx
<Divider />
<Divider label="2024" />
<Divider label="Exhibition" align="left" />
```

`align`: `center` (default) / `left`. Bare `<Divider/>` is just a rule.
