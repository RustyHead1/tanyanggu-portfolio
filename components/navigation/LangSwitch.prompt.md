The bilingual JA / EN language toggle from the site header.

```jsx
<LangSwitch value={lang} onChange={setLang} />
```

The active code is plain off-white; the inactive one fades to blue on hover. The site is JA-default — order `['JA','EN']`.
